// src/redux/reducer.js
const initialState = {
    // Define your initial state here
  };
  
  const rootReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define your actions and how they modify the state
      default:
        return state;
    }
  };
  
  export default rootReducer;